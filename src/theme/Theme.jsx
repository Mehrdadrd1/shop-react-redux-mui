import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      light: "#f8f0fb",
      main: "#e8c547",
      dark: "#8075ff",
    },
    secondary: {
      light: "#4d5061",
      main: "#6320ee",
      dark: "#30323d",
    },
  },
  typography: {
    fontFamily: {},
  },
});

export default theme;
